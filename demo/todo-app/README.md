# Demo — Todo App (agent-platform smoke test)

Mục tiêu: một demo project nhỏ để chạy end-to-end pipeline:

- onboarding → builder → coder → reviewer + tester + security → documenter

---

## Setup

```
pnpm install
pnpm dev
```

---

## Suggested tasks to try

1) `agent-onboarding: analyze this repo and create .agent/context`
2) `agent-builder: generate coder agents for this demo`
3) `agent-orchestrator: implement CRUD todos with tests`

