# Legacy Alias: AI-native

`ai-native` is a legacy alias for
[Eval-Driven AI Development](eval-driven-ai.md).

Use the canonical term in new documents and examples. Keep the alias only for backwards compatibility
with existing workspace configuration.
