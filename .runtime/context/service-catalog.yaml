version: 1
role: "service-control-plane"
generated_at: null
generated_by: null
status: "needs_onboarding"

description: "Defines coding target metadata. Actual source code lives in each service.path, normally services/<service-name>; service contracts and registries live under .runtime/context."

path_rules:
  preferred_layout: "Application repositories are cloned under services/<service-name>/ inside the agent-workspace workspace."
  preferred_relative_path: "services/<service-name>"
  do_not_assume: "Never assume service code lives outside services/ unless service.path explicitly says so."
  write_rule: "Generated coders may write only inside allowed_write_paths approved for their service."

services: []
agent_candidates: []
